// src/components/Hero.js
import React from 'react';
import '../App.css'; // Estilos para el componente Hero

const Hero = () => {
    return (
        <section className="hero-section">
        </section>
    );
};

export default Hero;
